"use client";

// Stars are unlimited — no upgrade banner needed.
// Keeping the component so layout imports don't break.
export function UpgradeBanner(_props: { aboveBottomBar?: boolean }) {
  return null;
}
